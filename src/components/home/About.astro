---
import { getConfigurationCollection } from "../../lib/utils";

const { data: config } = await getConfigurationCollection();
---

<h3 class="font-display text-lg leading-loose sm:text-xl">
  {config.about.title}
</h3>
<div class="zag-text zag-transition flex flex-col gap-4">
  <p set:html={config.about.description} />
  <a
    href={`mailto:${config.personal.email}`}
    class="underline underline-offset-8"
    >✉️ Let's Connect: {config.personal.email}</a
  >
  <a href={config.personal.cv} class="underline underline-offset-8"
    >📄 View CV</a
  >
</div>
<div class="mt-4 flex flex-col gap-2">
  <h4 class="font-display text-lg leading-loose sm:text-xl">
    {config.about.skillsTitle}
  </h4>
  <ul class="flex flex-wrap gap-2">
    {config.about.skills.map((skill) => <li class="tag">{skill}</li>)}
  </ul>
</div>
